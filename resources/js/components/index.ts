// Table Components
export { InertiaTableWrapper } from './Table/inertia-table-wrapper';

// Re-export table types for convenience
export type {
    Action,
    DynamicIconProps,
    EmptyStateProps,
    FilterProps,
    FilterState,
    PaginationProps,
    TableColumn,
    TableConfig,
    TableResource,
    UrlConfig,
} from '../types';
